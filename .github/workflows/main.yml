name: Test RAM Monitor

on:
  schedule:
    - cron: '0 1 * * *'   # 每天 1點跑一次
  workflow_dispatch:       # 也可以手動跑

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Run test.py
        run: python test.py
